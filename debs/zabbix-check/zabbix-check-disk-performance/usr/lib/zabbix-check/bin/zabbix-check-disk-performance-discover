#!/bin/bash

ls -al /dev/disk/by-id/ | grep -e name -e ata -e scsi | grep -v '\-part' | while read l; do

a=$(echo $l | awk '{print $9}')
b=$(echo $l | awk '{print $11}' | cut -c 7-)

if echo $a | grep ^dm >/dev/null; then
	echo $a | cut -c 9-
else
	echo $b
fi

done
